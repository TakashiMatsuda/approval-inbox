name = "approval-inbox"
main = "src/worker/worker.ts"
compatibility_date = "2026-08-01"

[[durable_objects.bindings]]
name = "INBOX"
class_name = "InboxDO"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["InboxDO"]

# Secrets (set via `wrangler secret put`): API_KEY, SIGNING_SECRET
# Optional vars: BASE_URL, NOTIFY_FORMAT, NOTIFY_ONE_TAP
# Optional secrets: NOTIFY_URL (an ntfy topic name is password-like), NOTIFY_AUTH
# See "Mobile notifications" in the README.
